{
  "buildCommand": "cd libreria-react && npm install && npm run build",
  "outputDirectory": "libreria-react/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
